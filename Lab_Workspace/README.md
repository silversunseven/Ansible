# Lab Setup

## Requirements
* This will setup a lab env based on existing VMs
* Add IP's and names to the script.
* Note: I have decided to use mulitple VM flavours (rhel,ubuntu and centos) 
* I use UTM VM manager for creating these
* I use zshrc as my shell but the script is written in bash
* I am using a macOS based system to setup this up. After this is setup I will primaryily use the control host to do any ansible work.

## What to do
1. Update the script with IP's and names/aliases
2. run ./pre-ansible.sh
3. run ./pre.mac.sh
